= Comandos básicos

Vamos inciar montando a estrutura inicial do nosso projeto a ser utilizado:

1. No lugar de preferência crie uma pasta com nome `GitGitHub`;
2. Abra o terminal na pasta em questão e então digite o seguinte comando para iniciar o versionamento dos arquivos presentes na pasta: `git init`;
2.1. Perceba a mensagem que aparece, pode ser que surjam dúvidas sobre, mas explique que o nome padrão é `master` mas possível alterar, mais detalhes sobre _branches_ posteriormente.
3. Crie um arquivo chamado `intro.md`
3.1. O arquivo criado foi um arquivo _Markdown_, uma linguagem de marcação para documentos.
4. Use o comando `git status`
4.1. 
5. Adicione o conteúdo:
```md
# Introdução

Batata quente 
```
5. Após isso use o comando `git status`
